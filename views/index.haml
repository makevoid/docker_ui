%h1 Containers

- if containers.empty?
  %p No containers available

%ul
  - for container in containers
    - container = Hashie::Mash.new container.info
    %li
      = container.to_yaml.split("\n")[1..-1].join("<br>")
